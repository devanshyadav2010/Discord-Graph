{
  "name": "discord-graph",
  "version": "1.0.0",
  "description": "An npm package for generating line graphs and storing stats (CPU, RAM, Servers) using QuickChart.js and a database (MongoDB or pro.db).",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["stats", "graphs", "npm", "quickchart", "mongodb", "pro.db"],
  "author": "Devansh Yadav",
  "license": "MIT",
  "dependencies": {
    "quickchart-js": "^2.0.2",
    "mongodb": "^4.3.2"
  }
}
